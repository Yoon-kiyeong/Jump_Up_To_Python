PI = 3.141592

class Math:
    def solv(self, r):
        return PI * (r ** 2)

def add(a,b):
    return a + b

#이 파일은 원의 넓이를 계산하는 Math 클래스와 두 값을 더하는 add 함수 그리고 원주율 값에 해당하는 PI 변수처럼 클래스, 함수, 변수 등을 모두 포함하고 있다.
#다음과 같이 따라해 보자.

